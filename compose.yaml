name: locallabel

services:
  locallabel:
    build: .
    image: locallabel:local
    ports:
      - "${LOCALLABEL_PORT:-8811}:8811"
    volumes:
      - locallabel-data:/data
    restart: unless-stopped

volumes:
  locallabel-data:
