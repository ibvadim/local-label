name: locallabel

services:
  locallabel:
    image: ghcr.io/ibvadim/local-label:latest
    ports:
      - "${LOCALLABEL_PORT:-8811}:8811"
    volumes:
      - locallabel-data:/data
    restart: unless-stopped

volumes:
  locallabel-data:
